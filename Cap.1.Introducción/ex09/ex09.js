/* ARRAYS O ARREGLOS

Un array es una estructura de datos que se utiliza para almacenar una colección ordenada de elementos.
Estos elementos pueden ser cadenas de texto, valores numericos como: enteros, decimales; valores booleanos, entre otros. Pueden ser tambien un conjunto de funciones e inclusive otros arrays.

*/

let numeros = []; // Declaración de un array vacío.

numeros = [15, 80, 650, 50.43, -10]; //Se guarda en el array una colección numerica de tipo decimal o entero
console.log(numeros); // Se imprime por consola todo el array
console.log(numeros[3]); // Se imprime por consola el 4to elemento que pertenece al índice 3. 